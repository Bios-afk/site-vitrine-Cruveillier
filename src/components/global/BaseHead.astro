---
import "../../styles/global.css";
import { Font } from "astro:assets";

export interface Props {
  title?: string;
  description?: string;
  url?: string;
  image?: string;
  themeColor?: string;
  type?: string;
}

const {
  title = "Anaïs Cruveiller",
  description = "Ecouter, comprendre, défendre telle est la devise du Cabinet d’Avocat Anaïs CRUVEILLER qui vous accueille en plein cœur de Bordeaux.",
  url = Astro.url.href,
  image = "/og.jpg",
  themeColor = "#0b0d07",
  type = "website",
} = Astro.props;

const siteOrigin = Astro.site ? Astro.site.href.replace(/\/$/, "") : "https://avocat-cruveiller.fr";
const absoluteImageUrl = image.startsWith("http")
  ? image
  : `${siteOrigin}${image}`;

const canonicalUrl = (() => {
  try {
    const u = new URL(url);
    u.search = "";
    u.hash = "";
    return u.href.replace(/\/$/, "") || u.href;
  } catch {
    return url;
  }
})();

const hasQueryString = (() => {
  try {
    return new URL(url).search.length > 0;
  } catch {
    return false;
  }
})();
const robotsContent = hasQueryString ? "noindex, follow" : "index, follow";
const fullTitle =
  title === "Anaïs Cruveiller" ? title : `${title} | Anaïs Cruveiller`;
---

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />

<title>{fullTitle}</title>
<meta name="description" content={description} />
<meta
  name="keywords"
  content="avocat bordeaux, cabinet d'avocat, anaïs cruveiller, conseil juridique, défense, droit"
/>
<meta name="author" content="Anaïs Cruveiller" />

<link rel="icon" href="/favicon.ico" sizes="32x32" />
<link rel="icon" type="image/svg+xml" href="/logo.svg" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />

<Font cssVariable="--font-poppins" />

<meta property="og:type" content={type} />
<meta property="og:site_name" content="Anaïs Cruveiller" />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image" content={absoluteImageUrl} />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:url" content={canonicalUrl} />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={absoluteImageUrl} />

<link rel="canonical" href={canonicalUrl} />

<meta name="robots" content={robotsContent} />
<meta name="googlebot" content={robotsContent} />

<meta name="theme-color" content={themeColor} />
<meta http-equiv="X-UA-Compatible" content="IE=edge" />

<meta name="generator" content={Astro.generator} />

<script is:inline>
  (function () {
    function applyTheme() {
      try {
        const stored = localStorage.getItem("nlm-theme");
        const prefersDark = window.matchMedia(
          "(prefers-color-scheme: dark)",
        ).matches;
        const dark = stored ? stored === "dark" : prefersDark;
        document.documentElement.classList.toggle("dark", dark);
      } catch (e) {}
      document.documentElement.classList.remove("no-js");
    }

    applyTheme();

    // Astro's ClientRouter (View Transitions) swaps the DOM on navigation
    // without re-running inline head scripts, which would otherwise drop the
    // theme class. Re-apply it before the new page paints so the theme
    // persists across page changes. document-level listeners survive swaps.
    document.addEventListener("astro:after-swap", applyTheme);
  })();
</script>

<script
  is:inline
  type="application/ld+json"
  set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "Organization",
    name: "Anaïs Cruveiller",
    url: siteOrigin,
    logo: `${siteOrigin}/logo.svg`,
    description,
  })}
/>
